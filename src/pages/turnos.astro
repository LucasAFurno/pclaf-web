---
import AppointmentStep from '../components/AppointmentStep.astro';
import { appointmentPageParts } from '../lib/appointment-page';

const { before, steps, after } = appointmentPageParts();
---
<Fragment set:html={before} />
{steps.map(html => <AppointmentStep {html} />)}
<Fragment set:html={after} />
