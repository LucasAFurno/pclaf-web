{
  "name": "pclaf-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "verify:output": "node scripts/verify-static-output.mjs",
    "verify:catalog": "node scripts/verify-service-catalog.mjs",
    "verify:sitemap": "node scripts/verify-sitemap.mjs",
    "check": "npm run build && npm run verify:output && npm run verify:catalog && npm run verify:sitemap"
  },
  "devDependencies": {
    "astro": "^5.16.0"
  }
}
